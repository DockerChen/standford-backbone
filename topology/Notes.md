## Emulate the Stanford-backbone network topology in Mininet:
* The script is updated to be compatible with Mininet 2.0+.
* Build the network by `python mininet_rebuilder.py`. By default, `localhost:6633` is used as the primary controller.